{
  "name": "woodsProduce",
  "version": "1.0.0",
  "description": "Website for a full line produce wholesaler in the mid Atlantic",
  "main": "index.js",
  "scripts": {
    "test": "eslint src && preact test",
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "preact build && preact serve",
    "dev": "preact watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "eslintConfig": {
    "extends": "eslint-config-synacor"
  },
  "devDependencies": {
    "eslint": "^4.1.1",
    "eslint-config-synacor": "^1.0.1",
    "if-env": "^1.0.0",
    "node-sass": "^4.5.3",
    "preact-cli": "^1.3.0",
    "sass-loader": "^6.0.6"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "preact": "^8.1.0",
    "preact-compat": "^3.16.0",
    "preact-router": "^2.5.2",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "react-player": "^0.18.0"
  }
}
